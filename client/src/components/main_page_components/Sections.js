import React from 'react';
import TypeBar from "../TypeBar";

const Sections = () => {
    return (
        <div>
            <h2 style={{textAlign: "center"}}>Популярные разделы</h2>
            <TypeBar/>
        </div>
    );
};

export default Sections;